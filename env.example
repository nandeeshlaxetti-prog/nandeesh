# Application Configuration
# Copy this file to .env and customize as needed

# Application Mode (desktop, web, server)
APP_MODE=desktop

# Desktop Runtime (electron, tauri, neutralino)
DESKTOP_RUNTIME=electron

# Queue Backend (local, redis, sqs, bull)
QUEUE_BACKEND=local

# Data Directory (optional - defaults to OS appdata path)
# DATA_DIR=/path/to/custom/data/directory

# Database Configuration
# DATABASE_URL=file:./database.db

# Node Environment
NODE_ENV=development

# Logging
LOG_LEVEL=info

# API Configuration
API_HOST=localhost
API_PORT=3000

# Security (optional)
# JWT_SECRET=your-jwt-secret-key
# ENCRYPTION_KEY=your-encryption-key

# Development specific
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# NEXT_PUBLIC_APP_NAME=LNN Legal Desktop
